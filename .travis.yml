os: osx
osx_image: xcode7.3

language: node_js

sudo: false

node_js:
    - '7.1.0'

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - npm install

script:
    - npm run unittest
